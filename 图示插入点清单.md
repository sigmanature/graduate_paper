# 图示插入点清单

说明：

1. 本清单面向当前三份重写稿：`第一二章重写稿.md`、`第三章开头重写稿.md`。
2. 行号基于当前文件版本，后续正文若继续改动，插图位置需重新校准。
3. `draw.io` 适合系统组件图、流程图、路径图；`TikZ` 更适合层级图、对比图、抽象机制图、学术风格示意图。
4. 优先级分为：`必须画`、`建议画`、`可选画`。

---

## 必须画

| 编号 | 文件 | 插入点 | 周围文字 | 图类型 | 推荐工具 | 作用 |
|---|---|---:|---|---|---|---|
| M1 | `第一二章重写稿.md` | 行 25 后 | 1.1 末尾，“基于这一三层递进关系，本文的技术路线可以概括为……” | 研究问题与技术路线总览图 | `TikZ` 优先，`draw.io` 也可 | 把“4KB 过小 → Large Folio → iomap → F2FS 专属扩展”一次讲清，是第一章最重要的总图 |
| M2 | `第一二章重写稿.md` | 行 103 后 | `2.1 页缓存与页描述符` 开头 | 分层架构图 | `TikZ` 优先 | 画清 `用户态/VFS/page cache/F2FS/bio/FTL/flash` 七层关系，第二章的地基图 |
| M3 | `第一二章重写稿.md` | 行 139 后 或清理重复后保留的 `2.5` 节标题后 | `2.5 走读例子：一次 64KB buffered read 的完整路径` | 时序/流程图 | `draw.io` 优先 | 将 read 路径按“系统调用→页缓存→F2FS→bio→设备→回调→用户态返回”画出来，直接支撑第二章叙事 |
| M4 | `第一二章重写稿.md` | 行 157 后 或清理重复后保留的 `2.6` 节标题后 | `2.6 Large Folio：可变阶数动态大页` | 对比图 | `TikZ` 优先 | 画 4KB page 路径与 64KB folio 路径的对比：对象数、锁次数、映射次数、bio 组织方式 |
| M5 | `第一二章重写稿.md` | 行 205 后 | `2.7 F2FS 的异地更新、GC 与压缩文件` | F2FS 关键机制总览图 | `draw.io` 优先 | 一图把 out-place update、NAT/SIT/SSA/Checkpoint、GC、compression 的关系串起来 |
| M6 | `第一二章重写稿.md` | 行 217 后 | `2.8 iomap：字节区间映射与 per-folio 状态追踪` | 区间映射机制图 | `TikZ` 优先 | 画“块级 buffer_head 模型”与“字节区间 iomap 模型”的抽象差异，以及 per-folio state 的位置 |
| M7 | `第三章开头重写稿.md` | 行 9 后 | `3.2 F2FS 所有路径的公共前提：一个页缓存对象等于一个块` | 问题源头图 | `TikZ` 优先 | 把“共同前提”画出来：读、写准备、写回、GC 四条路径如何都依赖 `1 folio = 1 block` 假设 |
| M8 | `第三章开头重写稿.md` | 行 47 后 | `3.5 脏页写回中的写放大与子范围控制` | 写回生命周期/写放大图 | `draw.io` 优先 | 画“局部 4KB 变脏 → 整 64KB 提交 → 日志空间消耗 → GC 压力”的因果链，以及正确的子范围控制逻辑 |

---

## 建议画

| 编号 | 文件 | 插入点 | 周围文字 | 图类型 | 推荐工具 | 作用 |
|---|---|---:|---|---|---|---|
| R1 | `第一二章重写稿.md` | 行 11 后 | 第一章中“4KB 页缓存粒度限制底层大块连续 I/O 能力” | 小型对照示意图 | `TikZ` | 用极简图说明 page cache 的 16 个 4KB 对象 vs 块层 1 次大 bio 的矛盾 |
| R2 | `第一二章重写稿.md` | 行 13 后 | THP 不适合作为文件页缓存直接方案 | 对比表图或约束图 | `TikZ` | 用于把 THP 与 Large Folio 的差异视觉化，避免文字过长 |
| R3 | `第一二章重写稿.md` | 行 211 后 | `GC 的基本流程是……` | GC 流程图 | `draw.io` | 单独把 victim 选择→读有效块→写新位置→更新 NAT→回收旧段画成流程，后面第四章 GC 适配会用到 |
| R4 | `第一二章重写稿.md` | 行 213 后 | `压缩以簇（cluster）为单位……` | cluster 与 folio 边界示意图 | `TikZ` | 为第三章 3.4 做前置铺垫 |
| R5 | `第三章开头重写稿.md` | 行 25 后 | `3.3 私有字段的所有权冲突` | 冲突图/崩溃路径图 | `draw.io` 或 `TikZ` | 画 `folio->private` 被 F2FS 当整数、被 iomap 当指针的双重解释，适合做论文中的关键问题图 |
| R6 | `第三章开头重写稿.md` | 行 37 后 | `3.4 压缩文件的簇边界与 Large Folio 边界的不重合` | 边界重叠示意图 | `TikZ` | 画 64KB folio 覆盖多个 16KB cluster，且子区间完成时机不同 |

---

## 可选画

| 编号 | 文件 | 插入点 | 周围文字 | 图类型 | 推荐工具 | 作用 |
|---|---|---:|---|---|---|---|
| O1 | `第一二章重写稿.md` | 行 49 后 | `1.2.3 F2FS 与闪存友好文件系统相关研究` | 研究现状分类图 | `TikZ` | 将“页粒度 / iomap / F2FS”三类相关工作画成文献分类图，可有可无 |
| O2 | `第一二章重写稿.md` | 行 173 后 | `FTL 在后台执行垃圾回收……` | FTL 映射与回收示意图 | `TikZ` | 如果你想强调 flash 物理约束，可单独画；否则用文字足够 |
| O3 | `第三章开头重写稿.md` | 行 59 后 | `3.6 本章小结` | 问题-对应方案映射图 | `TikZ` | 将 3.2/3.3/3.4/3.5 与 4.2/4.3/4.4/4.6 对应起来，为第四章过渡服务 |

---

## 当前最推荐的作图顺序

建议不要一次画完全部图，按论文成型价值排序：

1. `M1` 第一章研究问题与技术路线总览图
2. `M2` 第二章系统分层架构图
3. `M3` 第二章 64KB buffered read 走读流程图
4. `M4` 4KB page 与 Large Folio 对比图
5. `M5` F2FS 关键机制总览图
6. `M7` 第三章“1 folio = 1 block”共同前提图
7. `M8` 脏页写回写放大/生命周期图
8. `R5` private 字段冲突图
9. `R6` 压缩簇与 folio 边界图
10. `M6` iomap 区间映射图

---

## 需要先注意的正文问题

在正式画图前，建议先处理以下正文结构问题，否则图号和插入点会变动：

1. `第一二章重写稿.md` 中第二章存在重复节标题与重复内容：
   - `2.4 闪存逻辑地址空间与 FTL`
   - `2.5 走读例子：一次 64KB buffered read 的完整路径`
   - `2.6 Large Folio：可变阶数动态大页`

2. 建议先清理第二章重复段落，再最终确定图号和精确插入位置。

3. 第一章第 5 行存在重复短句“往往难以充分发挥当前闪存设备的顺序访问能力”。

这些不影响当前挑选插图位置，但会影响后续排版落地。
